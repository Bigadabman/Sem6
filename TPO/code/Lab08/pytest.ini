[pytest]
testpaths = tests
addopts = -v
markers =
    smoke: short critical checks
    search: search scenarios on the real site
    cookies: browser cookie scenarios
    screenshot: tests that save screenshots
    run_order(order): lightweight built-in ordering marker handled in conftest.py
